#!/usr/bin/env node
console.log('Manual runtime test: extract a built release, run start.cmd, enter your own API key, run a small local task, then run stop.cmd. This repository does not automate GUI/model-provider validation.');
